<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kapoocino.camera">

    <!-- 图片编辑Activity -->
    <activity android:name=".editimage.EditImageActivity"></activity>

    <application android:theme="@style/squarecamera__CameraFullScreenTheme">
        <activity
            android:name=".squarecamera.RuntimePermissionActivity"
            android:theme="@style/squarecamera__CameraFullScreenTheme.NoUI"></activity>
        <activity android:name=".cropimage.CropActivity"></activity>
    </application>

</manifest>